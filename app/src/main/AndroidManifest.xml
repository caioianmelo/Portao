<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.portao">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Portao">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-feature android:name="android.hardware.bluetooth" />
    <uses-feature android:name="android.hardware.wifi" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:icon="@drawable/ic_bluetooth"/>
    <uses-permission android:name="android.permission.BLUETOOTH" android:icon="@drawable/ic_bluetooth" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" android:icon="@drawable/ic_wifi_symbol"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" android:icon="@drawable/ic_wifi_symbol"/>
</manifest>